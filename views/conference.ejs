<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Conference Bookings</title>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500;1,600;1,700|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i" rel="stylesheet">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <link href="assets/css/style.css" rel="stylesheet">


    <style>
        body {
            background-color: #f8f9fa;
            color: #343a40;
            padding: 20px;
        }

        #header {
            background-color: #343a40;
            color: #ffffff;
         }
        table {
            margin-top: 20px;
        }

        th, td {
            text-align: center;
        }

        .container {
            margin-top: 8rem;
        }
    </style>
</head>
<body>
      <!-- ======= Header ======= -->
  <header id="header" class="fixed-top d-flex align-items-cente">
    <div class="container-fluid container-xl d-flex align-items-center justify-content-lg-between">

      <h1 class="logo me-auto me-lg-0"><a href="/dashboard">Savannah Vintage Lounge</a></h1>
      <!-- Uncomment below if you prefer to use an image logo -->
      <!-- <a href="index.html" class="logo me-auto me-lg-0"><img src="assets/img/logo.png" alt="" class="img-fluid"></a>-->

      <nav id="navbar" class="navbar order-last order-lg-0">
        <ul>
          <li><a class="nav-link scrollto active" href="/dashboard">Dashboard</a></li>
        </ul>
        <!-- <div class="languages d-none d-md-flex align-items-center">
          <a href="/admin-login">Admin</a>

        </div> -->
        <i class="bi bi-list mobile-nav-toggle"></i>
      </nav><!-- .navbar -->


    </div>
  </header><!-- End Header -->


    <div class="container">
        <h2>Conference Bookings</h2>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>From Date</th>
                    <th>To Date</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i = 0; i < bookings.length; i++) { %>
                    <tr>
                        <td><%= bookings[i].name %></td>
                        <td><%= bookings[i].email %></td>
                        <td><%= bookings[i].phone %></td>
                        <td><%= bookings[i].from_date %></td>
                        <td><%= bookings[i].to_date %></td>
                        <td>
                            <form action="/delete-conference/<%= bookings[i].id %>" method="post">
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
