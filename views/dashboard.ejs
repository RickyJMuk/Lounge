<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Savannah Vintage Lounge</title>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500;1,600;1,700|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i" rel="stylesheet">

    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <style>
        body {
            font-family: "Open Sans", sans-serif;
            background: #0c0b09;
            color: #fff;
        }
        .dashboard-header {
            background-color: #1a1814;
            padding: 20px 0;
            margin-bottom: 30px;
        }
        .dashboard-title {
            font-family: "Playfair Display", serif;
            font-weight: 700;
            font-size: 32px;
            color: #cda45e;
        }
        .nav-link {
            color: #fff;
            transition: 0.3s;
        }
        .nav-link:hover {
            color: #cda45e;
        }
        .card {
            background-color: #1a1814;
            border: none;
            margin-bottom: 20px;
        }
        .card-header {
            background-color: #0c0b09;
            color: #cda45e;
            font-family: "Playfair Display", serif;
        }
        .btn-primary {
            background-color: #cda45e;
            border-color: #cda45e;
        }
        .btn-primary:hover {
            background-color: #d3b876;
            border-color: #d3b876;
        }
        .btn-danger {
            background-color: #8a2d2d;
            border-color: #8a2d2d;
        }
        .btn-danger:hover {
            background-color: #a13535;
            border-color: #a13535;
        }
    </style>
</head>
<body>
    <header class="dashboard-header">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h1 class="dashboard-title">Admin Dashboard</h1>
                </div>
                <div class="col-md-6 text-right">
                    <a href="/logout" class="btn btn-danger">Logout</a>
                </div>
            </div>
        </div>
    </header>

    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <div class="card">
                    <div class="card-header">Navigation</div>
                    <div class="card-body">
                        <nav class="nav flex-column">
                            <a class="nav-link active" href="/admin">Table Bookings</a>
                            <a class="nav-link" href="/conference">Conference Bookings</a>
                            <!-- Add more navigation items as needed -->
                        </nav>
                    </div>
                </div>
            </div>
            <div class="col-md-9">
                <div class="card">